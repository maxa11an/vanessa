{% for msg in flash('error') %}
    $.notify({
        message: "{{ msg }}"
    },{
        type: 'danger'
    });
{% endfor %}
{% for msg in flash('success') %}
    $.notify({
    message: "{{ msg }}"
    },{
    type: 'success'
    });
{% endfor %}
